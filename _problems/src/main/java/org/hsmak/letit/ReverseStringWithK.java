package org.hsmak.letit;

public class ReverseStringWithK {
    // ToDo - Fix the Logic
    public String reverseFirstKEvery2K(String s, int k) {
        /*char[] substr = s.substring(0, k).toCharArray();
        for (int i = 0; i < k / 2; i++) {
            char front = substr[i];
            substr[i] = substr[k - 1 - i];
            substr[k - 1 - i] = front;
        }

        return String.valueOf(substr) + s.substring(k);*/
        return "";
    }
}
